This directory contains `Three.js` components used by the game, such as player, enemies, camera, renderer, map, and obstacles.
Each component is responsible for creating and configuring a single part of the scene and can be imported into `main.js`.