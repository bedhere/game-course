import { defineConfig } from 'vite';
//import { viteSingleFile } from 'vite-plugin-singlefile';

export default defineConfig({
  // plugins: [viteSingleFile()],
  // base: './', // Makes paths relative so it works without a server root
});
